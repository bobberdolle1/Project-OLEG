# Олег — Telegram-бот с личностью, модерацией и играми (Версия 3.0)

Многофункциональный ИИ-ассистент и развлекательный бот для Telegram-чата с уникальной личностью, функциями модерации, автоматической генерацией контента и игровыми механиками.

**Нововведения в версии 3.0:**
- **Удалена устаревшая веб-панель** - вся административная функциональность теперь через Telegram
- **Система "Plug & Play"** - бот автоматически настраивается при добавлении в чат, сканирует описание и генерирует индивидуальный промпт
- **Умное приветствие пользователей** - с подтверждением "Я не бот" через inline-кнопку
- **Мозг Олега (RAG)** - система контекстного обучения и памяти на базе ChromaDB с использованием модели glm-4.6:cloud
- **Модуль зрения** - анализ изображений с помощью мультимодальной модели qwen3-vl:4b-instruct
- **Рандомные ответы** - бот сам вставляет реплики в чат при определенных условиях
- **Администрирование через личные сообщения** - полноценная админ-панель с reply-кнопками
- **Динамические режимы модерации** - переключение между "Лайт", "Норма", "Диктатура" с разными уровнями строгости
- **Обновленная документация**

**Стек технологий:**
- **Backend:** Python 3.10+, aiogram v3 (асинхронный Telegram API)
- **Database:** SQLAlchemy async + SQLite (или PostgreSQL)
- **Векторная БД:** ChromaDB для RAG
- **ИИ Модели:**
  - **Основная:** deepseek-v3.1:671b-cloud (для текстовых ответов)
  - **Визуальная:** qwen3-vl:4b-instruct (для анализа изображений)
  - **Память/RAG:** glm-4.6:cloud (для поиска в базе знаний)
- **Scheduler:** APScheduler для плановых задач
- **Логирование:** Встроенная система с ротацией логов

## ⚡ Быстрый старт

### Вариант 1: Docker Compose (рекомендуется) 🐳

```bash
# Клонировать и перейти в папку
git clone <url> && cd oleg-bot

# Скопировать конфиг
cp .env.docker .env

# Отредактировать .env (добавить TELEGRAM_BOT_TOKEN и PRIMARY_CHAT_ID)
nano .env

# Запустить (бот + Ollama в контейнерах)
docker-compose up -d

# Просмотр логов
docker-compose logs -f oleg-bot
```

**Все просто:** контейнеры, БД, Ollama запускаются автоматически!

📖 [Полное руководство Docker](./DOCKER.md)

---

### Вариант 2: Poetry (Python)

```bash
# Установить Poetry (если еще нет)
curl -sSL https://install.python-poetry.org | python3 -

# Клонировать проект
git clone <url> && cd oleg-bot

# Установить зависимости
poetry install

# Активировать виртуальное окружение
poetry shell

# Скопировать конфиг
cp .env.example .env

# Редактировать .env
nano .env

# Запустить бота
poetry run python -m app.main
```

**Преимущества Poetry:**
- ✅ Управление версиями зависимостей (`pyproject.toml`)
- ✅ Автоматическое виртуальное окружение
- ✅ Легко добавлять новые пакеты: `poetry add <package>`
- ✅ Воспроизводимые сборки: `poetry.lock`

---

### Вариант 3: Традиционный pip

```bash
# Требования: Python 3.10+, Ollama установлен локально

git clone <url> && cd oleg-bot

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate
# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Конфигурация
cp .env.example .env
nano .env

# Запуск
python -m app.main
```

---

### 1. Требования (для pip)
- Python 3.10 или выше
- Ollama с установленной моделью `deepseek-v3.1:671b-cloud`
- Telegram Bot Token (получить в [@BotFather](https://t.me/BotFather))

### 2. Конфигурация
Скопируйте `.env.example` в `.env` и заполните значения:
```bash
cp .env.example .env
```

**Основные параметры:**
- `TELEGRAM_BOT_TOKEN` — токен бота (обязателен)
- `PRIMARY_CHAT_ID` — ID основного чата
- `SUMMARY_TOPIC_ID` — ID темы для ежедневного пересказа
- `CREATIVE_TOPIC_ID` — ID темы для контента
- `OLLAMA_BASE_URL` — URL Ollama (по умолчанию localhost:11434)
- `LOG_LEVEL` — уровень логирования (DEBUG/INFO/WARNING/ERROR)

Логи сохраняются в `logs/oleg.log` (создается автоматически).

---

## 🎯 Основные возможности

### 1. **Q&A с личностью "Олег"**
- Отвечает на вопросы в грубоватой, прямой манере
- Активируется при упоминании бота (`@botname`) или ответе на его сообщение
- Интеграция с Ollama для умной генерации ответов
- Retry-логика при сбое подключения

### 2. **Ежедневный пересказ чата** (#dailysummary)
**Время:** 08:00 МСК

Бот анализирует сообщения за последние 24 часа и:
- Группирует обсуждения в 3-5 ключевых тем (умная классификация по ключевым словам)
- Подсчитывает количество сообщений по каждой теме
- Собирает все опубликованные ссылки
- Публикует форматированный отчет с эмодзи

**Пример вывода:**
```
📆 Что обсуждалось вчера [26.11.2025]
🎮 Steam Deck (24 сообщения)
🔥 Видеокарты (18 сообщений)
⚡ Процессоры (12 сообщений)

Интересные ссылки:
https://example.com/link1
https://example.com/link2
#dailysummary
```

### 3. **Генерация креативного контента**
**Время:** 20:00 МСК

Бот случайно выбирает один из двух форматов:

**Формат 1: Сборник цитат**
- 6 вымышленных, матерных, ироничных цитат
- На рандомные темы про железо/разгон/Steam Deck
- Участие никнеймов активных пользователей

**Формат 2: Абсурдная история**
- 120-200 слов про события чата
- Рандомные сценарии конфликтов/приключений (>10 вариантов)
- С участием реальных пользователей чата

**Публикуется в:** тема для мемов

### 4. **Команды модерации** (только для админов)

```bash
# Банить пользователя (ответом на сообщение)
олег бан @user 1д причина

# Варианты времени: 10м, 1ч, 1д, навсегда
олег бан @user навсегда спам

# Замьютить (запретить отправлять сообщения)
олег мут @user 1ч оскорбления

# Кикнуть из чата
олег кик @user флуд
```

### 5. **Игровые механики**

#### `/grow` — Увеличить "пиписю"
- Случайный прирост: 1-20 см
- Кулдаун: 12-24 часа
- Выводит текущий размер и место в топе
- Красивое форматирование с эмодзи

#### `/top` — Топ-10 игроков
- Показывает лидеров по размеру
- С номерами и текущим размером

#### `/pvp @username` — Дуэль
- Победитель определяется по размеру + рандом
- Победитель "крадет" 10-30% размера у проигравшего
- Требуется /grow для обоих перед дуэлью
- Результаты всегда справедливы и развлекательны

#### `/casino [ставка]` — Слоты
- Ставки от 1 до 1000 (по умолчанию 10)
- Символы: 🍒 🍋 🔧 🧰 🎮 🔥
- 3 одинаковых символа = x5 выигрыш (джекпот!)
- 2 одинаковых = x2 выигрыш
- Виртуальная валюта (начальный баланс 100)

### 6. **Антираид**
Автоматическое обнаружение рейдов:
- Отслеживает вход новых пользователей
- При >5 входов за 1 минуту активирует защиту
- Новые пользователи временно замьючиваются на 1 час
- Админы могут одобрить через кнопку "Размьютить"
- Защита активна 15 минут

---

## 📁 Структура проекта

```
Project-OLEG/
├── app/
│   ├── __init__.py
│   ├── main.py                 # Точка входа, инициализация бота
│   ├── config.py               # Конфигурация из .env
│   ├── logger.py               # Система логирования (новый файл)
│   ├── database/
│   │   ├── __init__.py
│   │   ├── session.py          # SQLAlchemy async сессии
│   │   └── models.py           # ORM модели (User, GameStat, etc.)
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── qna.py              # Q&A обработчик (улучшенная логика)
│   │   ├── games.py            # Игровые команды (документация добавлена)
│   │   ├── moderation.py       # Команды модерации (документация)
│   │   └── antiraid.py         # Антираид система
│   ├── middleware/
│   │   ├── __init__.py
│   │   └── logging.py          # Логирование всех сообщений и ссылок
│   ├── services/
│   │   ├── __init__.py
│   │   └── ollama_client.py    # API интеграция с Ollama (retry, расширенные промпты)
│   └── jobs/
│       ├── __init__.py
│       └── scheduler.py        # Плановые задачи (пересказ/контент)
├── data/                        # БД SQLite (создается автоматически)
├── logs/                        # Логи (создается автоматически)
├── .env.example                 # Шаблон переменных окружения (расширенный)
├── .env                         # Переменные окружения (в .gitignore)
├── requirements.txt             # Зависимости Python
└── README.md                    # Этот файл
```

---

## 🔧 Расширенная конфигурация

### Переменные окружения

| Переменная | По умолчанию | Описание |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | — | Токен бота (обязателен) |
| `PRIMARY_CHAT_ID` | — | ID основного чата (обязателен) |
| `SUMMARY_TOPIC_ID` | 1 | ID темы для пересказа (#dailysummary) |
| `CREATIVE_TOPIC_ID` | 1121 | ID темы для контента (истории/цитаты) |
| `OLLAMA_BASE_URL` | http://localhost:11434 | URL сервера Ollama |
| `OLLAMA_MODEL` | deepseek-v3.1:671b-cloud | Модель для генерации |
| `OLLAMA_TIMEOUT` | 90 | Таймаут запросов (сек) |
| `DATABASE_URL` | sqlite+aiosqlite:///./data/oleg.db | Строка подключения БД |
| `TIMEZONE` | Europe/Moscow | Часовой пояс для планировщика |
| `LOG_LEVEL` | INFO | DEBUG/INFO/WARNING/ERROR/CRITICAL |
| `LOG_FILE` | logs/oleg.log | Путь к файлу логов |

### Переключение на PostgreSQL
Измените `DATABASE_URL`:
```
postgresql+asyncpg://user:password@localhost:5432/oleg
```

---

## 🤖 Личность бота

Олег — грубоватый, прямолинейный, матерящийся, но полезный ассистент. Говорит коротко, по делу, с иронией и сленгом. Стиль общения адаптирован для чатов про технику, разгон, Steam Deck и железо.

**Системный промпт (определен в `app/services/ollama_client.py`):**
```
Ты — Олег, грубоватый, прямолинейный, матерящийся, но полезный бот.
Говори коротко, по делу, с иронией и сленгом. Можешь ругнуться, но
без оскорблений по запрещённым признакам. Контекст — чаты про технику,
разгон, Steam Deck, железо. Помогай чётко, без воды.
```

---

## 📊 Логирование

Все события логируются с разными уровнями:
- **INFO:** Основные события (запуск, пересказ, творчество, команды)
- **WARNING:** Проблемы с переподключением к Ollama
- **ERROR:** Критические ошибки и исключения
- **DEBUG:** Детальная информация о каждом действии

**Хранение логов:**
- Путь: `logs/oleg.log` (согласно конфигу)
- Ротация: максимум 10 MB, до 5 файлов
- Формат: `[YYYY-MM-DD HH:MM:SS] LEVEL [module]: message`

**Примеры логируемых событий:**
```
[2025-11-26 14:23:45] INFO [app.main]: Инициализация базы данных...
[2025-11-26 14:23:46] INFO [app.jobs.scheduler]: Планировщик запущен
[2025-11-26 14:24:15] INFO [app.handlers.games]: Grow: @username +12 cm (total: 47)
[2025-11-26 14:25:00] WARNING [app.services.ollama_client]: Ollama timeout (попытка 1/2)
[2025-11-26 14:25:05] ERROR [app.services.ollama_client]: Ollama request failed
```

---

## ⚠️ Известные ограничения и требования

1. **Ollama должна быть запущена** перед стартом бота
   ```bash
   ollama serve
   ```

2. Модель должна быть предварительно загружена:
   ```bash
   ollama pull deepseek-v3.1:671b-cloud
   ```

3. Большие модели требуют достаточно VRAM (рекомендуется 6+ GB)

4. В SQLite есть ограничения на конкурентность (для продакшена используйте PostgreSQL)

5. Требуется установленный Python 3.10+

---

## 🚀 Развертывание

### Локальный запуск (разработка)
```bash
python -m app.main
```

### Docker (рекомендуется для продакшена)
```dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["python", "-m", "app.main"]
```

Запуск:
```bash
docker build -t oleg-bot .
docker run -d \
  -e TELEGRAM_BOT_TOKEN=your_token \
  -e PRIMARY_CHAT_ID=your_chat_id \
  --net=host \
  oleg-bot
```

### Systemd сервис (Linux)
Создайте файл `/etc/systemd/system/oleg-bot.service`:
```ini
[Unit]
Description=Oleg Telegram Bot
After=network.target

[Service]
Type=simple
User=bot
WorkingDirectory=/home/bot/oleg
EnvironmentFile=/home/bot/oleg/.env
ExecStart=/usr/bin/python3 -m app.main
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Управление:
```bash
sudo systemctl enable oleg-bot       # Автозапуск
sudo systemctl start oleg-bot        # Запуск
sudo systemctl stop oleg-bot         # Остановка
sudo systemctl status oleg-bot       # Статус
sudo journalctl -u oleg-bot -f       # Логи в реальном времени
```

---

## 📝 Что было улучшено

### ✅ Новые возможности (Фаза 1)
- Рандомные сценарии для историй (10+ шаблонов вместо одного)
- Рандомные темы для цитат (15+ тем)
- Умная классификация тем по ключевым словам
- Эмодзи для каждой темы в пересказе

### ✅ Стабильность (Фаза 2)
- Retry-логика для Ollama (2 попытки)
- Graceful fallback при ошибках подключения
- Comprehensive логирование всех событий
- Правильная обработка таймаутов и исключений

### ✅ Качество кода (Фаза 3)
- Рефакторинг логики Q&A (функция `_should_reply`)
- Подробная документация всех обработчиков
- Типизированные функции
- Константы для баланса игр в одном месте

### ✅ Конфигурация (Фаза 4)
- Расширенный `.env.example` с комментариями
- Новые параметры: `OLLAMA_TIMEOUT`, `LOG_LEVEL`, `LOG_FILE`
- Поддержка PostgreSQL в примере

### ✅ Логирование (Фаза 5)
- Новый модуль `app/logger.py` с ротацией
- Форматированные логи с временными метками
- Логирование всех критических операций

---

## 💡 Дальнейшие улучшения

Полный roadmap и направления для разработки: [CONTRIBUTING.md](./CONTRIBUTING.md)

Быстро:
- [ ] Система достижений и бейджей
- [ ] Торговля между игроками
- [ ] Веб-панель управления и статистики
- [ ] Система рангов и репутации
- [ ] Unit-тесты и CI/CD
- [ ] Kubernetes и Prometheus метрики
- [ ] Поддержка нескольких языков

---

## 📞 Поддержка и вопросы

Если у вас есть вопросы или проблемы:
1. Проверьте логи в `logs/oleg.log`
2. Убедитесь, что Ollama запущена и доступна
3. Проверьте значения в `.env`

---

## 📝 Лицензия

Проект для личного использования. Используйте в соответствии с политиками Telegram.
