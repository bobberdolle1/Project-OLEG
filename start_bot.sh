#!/bin/bash
# Скрипт запуска бота "Олег" (Версия 3.0)

echo "🚀 Запуск бота 'Олег' (Версия 3.0)"
echo "====================================="

# Проверяем, установлен ли Poetry
if ! command -v poetry &> /dev/null; then
    echo "❌ Poetry не установлен. Устанавливаю..."
    curl -sSL https://install.python-poetry.org | python3 -
    export PATH="$HOME/.local/bin:$PATH"
fi

# Устанавливаем зависимости
echo "📦 Установка зависимостей..."
poetry install

# Проверяем наличие .env файла
if [ ! -f ".env" ]; then
    echo "⚠️  Файл .env не найден, создаю из примера..."
    cp .env.example .env
    echo "👉 Пожалуйста, настройте .env файл с вашими токенами и настройками"
    exit 1
fi

# Проверяем обязательные переменные
if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
    export $(grep -v '^#' .env | xargs)
fi

if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
    echo "❌ TELEGRAM_BOT_TOKEN не установлен в .env файле"
    echo "👉 Добавьте ваш токен от @BotFather в .env файл"
    exit 1
fi

# Создаем директорию для логов, если не существует
mkdir -p logs

# Создаем директорию для данных, если не существует
mkdir -p data

# Запускаем бота
echo "🤖 Запуск бота..."
poetry run python -m app.main

echo "💤 Бот остановлен"